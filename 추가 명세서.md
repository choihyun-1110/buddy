### Gemini가 쓴거.
알약이 보이지 않는 상황(손 안에 숨겨짐)에서 **'손 → 입'** 동작과 **'물 마시기'** 동작의 **순차적 연결**을 통해 복용을 확정 짓는 로직은 매우 실무적이고 훌륭한 접근입니다.

이 로직을 Cursor가 이해할 수 있도록 **상태 머신(State Machine)** 기반의 설계 명세서로 작성해 드립니다. 이 내용을 `.md` 파일로 저장해서 전달하시면 됩니다.

---

## 📄 pill_ingestion_sequence_logic.md

# Pill Ingestion Event Sequence Logic Spec

## 1. Objective

알약 자체가 카메라에 보이지 않더라도, **[손의 입 접근] → [컵의 입 접근(물 마시기)]**으로 이어지는 일련의 행동 시퀀스를 감지하여 알약 복용 여부를 최종 판정한다.

## 2. Detection Targets

시스템은 다음 객체들을 실시간으로 추적해야 함:

* **Mouth ROI**: (이전 명세서에서 설계한 MouthROITracker 활용)
* **Hand**: 손가락 또는 손바닥 전체 (MediaPipe Hand 또는 YOLOv8/11)
* **Cup**: 물컵, 물병 등 (YOLOv8/11 또는 전용 분류기)

## 3. Finite State Machine (FSM)

복용 프로세스를 4단계의 상태로 정의한다.

| 상태 | 명칭 | 조건 (Trigger) | 설명 |
| --- | --- | --- | --- |
| **State 0** | **IDLE** | 기본 상태 | 사용자가 화면에 등장함 |
| **State 1** | **HAND_TO_MOUTH** | `overlap(Hand, Mouth_ROI) > threshold` | 손이 입에 닿음 (알약을 넣는 동작으로 간주) |
| **State 2** | **CUP_TO_MOUTH** | `State 1` 이후 N초 이내에 `overlap(Cup, Mouth_ROI) > threshold` | 손이 떨어진 후 물컵을 입에 가져다 댐 |
| **State 3** | **INGESTION_COMPLETE** | `State 2`가 T초 이상 지속 후 Cup 이탈 | 물을 마시는 동작 완료 (최종 복용 성공) |

## 4. Detailed Logic & Constraints

### 4.1 State 1: Hand-to-Mouth (Intent)

* **알약 미보임 허용**: 알약(Pill) 객체가 직접 검출되지 않아도 `Hand`와 `Mouth ROI`가 겹치면 '복용 시도'로 판단한다.
* **임계값**: 손의 중심점 또는 손의 Bounding Box가 Mouth ROI와 30% 이상 겹칠 때 발생.
* **타임아웃**: State 1 발생 후 10초 이내에 State 2가 발생하지 않으면 다시 IDLE로 초기화.

### 4.2 State 2: Cup-to-Mouth (Confirmation)

* **연속성**: 반드시 State 1을 거쳐야만 State 2로 진입할 수 있다. (단순히 물만 마시는 동작과 구분)
* **체류 시간**: 컵이 입 주변에 머무는 시간(Drinking duration)을 측정한다. (최소 1.5초 이상 권장)

### 4.3 State 3: Final Success

* 컵이 Mouth ROI에서 멀어지는 순간 `Ingestion Confirmed` 이벤트를 발생시킨다.
* 결과값으로 `복용 시각`, `총 소요 시간`, `신뢰도`를 반환한다.

## 5. Exception Handling (Robustness)

* **Sequence Break**: 컵을 들기 전에 손이 입에 너무 자주 왔다 갔다 하는 경우(간식 먹기 등)를 대비해, State 1과 State 2 사이의 시간 간격을 좁게 설정(예: 최대 15초).
* **False Positive**: 손이나 컵이 단순히 입 근처를 지나는 것과 '멈춰서 동작하는 것'을 구분하기 위해 각 상태 진입 시 최소 프레임 카운트(예: 5~10 FPS 유지)를 적용한다.
* **Occlusion**: 컵이 입을 가릴 때 FaceMesh가 깨질 수 있으므로, State 2 진입 직전의 Mouth ROI 위치를 고정(Lock)하여 판정에 사용한다.

## 6. Interface Specification (Class Design)

```python
class IngestionSequenceDetector:
    def __init__(self):
        self.state = "IDLE"
        self.timer = 0
        self.config = {
            "max_wait_for_cup": 10.0, # 손 이후 컵 대기 시간 (초)
            "min_drinking_time": 1.5   # 최소 물 마시는 시간 (초)
        }

    def update(self, hand_bbox, cup_bbox, mouth_roi, current_time):
        # 1. State Machine 로직 구현
        # 2. Overlap 계산 로직
        # 3. 타임아웃 체크
        return {
            "current_state": self.state,
            "event_triggered": False,
            "message": ""
        }

```

## 7. Deliverables for Cursor

* `IngestionSequenceDetector` 클래스 구현.
* 각 상태 변화를 화면 상단에 텍스트로 표시 (예: "READY" -> "PILL IN" -> "DRINKING" -> "SUCCESS").
* 모든 시퀀스가 완료되면 녹화된 프레임이나 로그를 저장하는 기능.

---

### 💡 Cursor 활용 팁:

1. **이전 명세서와 연결:** Cursor에게 위 내용을 줄 때, "방금 만든 `MouthROITracker`를 임포트해서 이 시퀀스 로직을 완성해줘"라고 말하세요.
2. **객체 검출 모델:** "Hand와 Cup 검출은 이미 되어 있다고 가정하거나, `ultralytics`의 YOLOv8 기본 모델을 사용해서 예시 코드를 짜줘"라고 요청하면 즉시 실행 가능한 코드를 얻을 수 있습니다.
3. **가이드:** 특히 **"알약이 손에 가려져 안 보일 수 있다는 점"**을 Cursor에게 다시 한번 강조하면, 알약 검출 실패 시에도 시퀀스가 끊기지 않도록 `Exception Handling`을 더 잘 짜줍니다.

다음으로 **"알약이 입에 들어가는 순간의 찰나(Occlusion vs Ingestion)"**를 구분하는 딥러닝 기반 판단 로직이 필요하시면 말씀해 주세요!_

### gpt가 쓴거
# Pill Intake Recognition Spec (Hand→Mouth→Cup→Swallow)  
**Target:** 영상에서 “알약 복용 완료(O)” vs “미복용(X/UNKNOWN)” 판정  
**Constraint:** 알약(pill)은 손에 쥐어져 **카메라에서 거의 보이지 않음**(pill detection 비의존 설계)

---

## 0) High-level Idea

“알약이 보이냐?”가 아니라, **행동 순서(sequence) + 증거(evidence) 누적**으로 판정한다.

복용 완료(O)의 최소 조건:
1) **Hand-to-mouth 이벤트** (손이 입 주변 ROI로 접근/접촉)
2) 이어서 **Cup-to-mouth 이벤트** (컵이 입 주변 ROI로 접근/접촉)
3) Cup-to-mouth 이후 **Swallow cue** (턱/입술/목 움직임) *또는* Cup contact 지속 시간 조건  
→ 이 3개를 **시간창(window)** 내에서 만족하면 “복용 완료(O)”

---

## 1) Inputs / Outputs

### Input
- RGB video stream (webcam)
- FPS known or estimated

### Output (per frame + final decision)
- `state`: current FSM state
- `events`: detected events with timestamps
- `final_decision`: `{O, X, UNKNOWN}`
- `debug`: ROI boxes + scores

---

## 2) Components

### 2.1 Face & Mouth ROI Tracker (필수)
- **Primary:** FaceMesh로 mouth ROI 추적
- **Fallback:** Face detector bbox 기반 비율 ROI
- **Hold:** Face/landmark 실패 시 이전 ROI를 K 프레임까지만 유지
- **Invalid:** K 초과 시 `roi_valid=False` (고정 화면 ROI 사용 금지)

Output:
- `mouth_roi = [x1,y1,x2,y2]`
- `roi_valid: bool`
- `roi_mode: {FACEMESH, FALLBACK, HOLD, INVALID}`
- EMA smoothing on ROI coords (alpha 0.4~0.6)

---

### 2.2 Hand Detector / Tracker (필수)
목적: “손이 입으로 갔다”를 잡기 위해 손을 detect/track

권장:
- MediaPipe Hands 또는 경량 hand detector (온디바이스 친화)
Output:
- `hand_bbox` 또는 `hand_keypoints`
- `hand_conf`

핵심 feature:
- `hand_to_mouth_dist` (손 중심/손끝 keypoint ↔ mouth_roi center)
- `hand_in_mouth_roi` (손 bbox/손끝이 mouth_roi와 overlap)

---

### 2.3 Cup Detector / Tracker (필수)
알약이 안 보이므로 “컵으로 삼킴”이 복용의 강한 증거임.

권장:
- 경량 object detector (cup class) 또는 custom fine-tune
Output:
- `cup_bbox`, `cup_conf`

핵심 feature:
- `cup_in_mouth_roi` (컵 bbox가 mouth_roi와 overlap)
- `cup_contact_duration` (연속 overlap 프레임 수)
- `cup_to_mouth_dist`

---

### 2.4 Swallow Cue Estimator (권장, 성능 핵심)
컵이 입에 닿은 뒤 “삼킴”을 영상 신호로 확인.

가능한 신호(가벼운 것부터):
1) **Lip closure**: FaceMesh 입술 landmark 상/하 거리 감소 패턴
2) **Jaw motion**: 턱 landmark의 y-velocity spike
3) (가능하면) **Throat motion**: 목 주변 landmark (정면일 때만 신뢰)

Output:
- `swallow_score` (0~1)
- `swallow_event` if score > threshold

Fallback:
- swallow cue가 불안하면 cup_contact_duration 기반으로 보완

---

## 3) Event Definitions (정의가 구현의 전부)

### 3.1 Hand-to-mouth Event (`E_HAND_MOUTH`)
Trigger 조건 (roi_valid=True일 때만):
- `hand_in_mouth_roi == True` 연속 `>= H_min_frames`  
  OR  
- `hand_to_mouth_dist < D_hand` 연속 `>= H_min_frames`

권장 파라미터:
- `H_min_frames = 4~8` (FPS 30 기준 0.13~0.27s)
- `D_hand = mouth_roi_width * 0.25` (상대값)

Event 기록:
- timestamp `t_hand`
- hand_conf, roi_mode

---

### 3.2 Cup-to-mouth Event (`E_CUP_MOUTH`)
Trigger 조건 (roi_valid=True일 때만):
- `cup_in_mouth_roi == True` 연속 `>= C_min_frames`

권장:
- `C_min_frames = 6~12` (0.2~0.4s)

Event 기록:
- timestamp `t_cup`
- cup_conf, contact_duration

---

### 3.3 Swallow Event (`E_SWALLOW`)
Trigger 조건:
- Cup-to-mouth 직후 window 내에서:
  - `swallow_score >= S_th`  
  OR  
  - `cup_contact_duration >= C_swallow_frames` (fallback)

권장:
- `S_th = 0.7` (초기값)
- `C_swallow_frames = 12~20` (0.4~0.7s)

Event 기록:
- timestamp `t_swallow`
- swallow_score

---

## 4) Finite State Machine (FSM)

States:
- `S0_IDLE`
- `S1_FACE_OK` (roi_valid 확보)
- `S2_HAND_MOUTH_DONE`
- `S3_CUP_MOUTH_DONE`
- `S4_SWALLOW_DONE` (복용 완료)
- `Sx_FAIL` (시간 초과/신뢰도 부족)

### Transitions
1) `S0_IDLE -> S1_FACE_OK`
   - roi_valid=True 연속 >= F_min_frames

2) `S1_FACE_OK -> S2_HAND_MOUTH_DONE`
   - `E_HAND_MOUTH` 발생

3) `S2_HAND_MOUTH_DONE -> S3_CUP_MOUTH_DONE`
   - `E_CUP_MOUTH` 발생  
   - 조건: `t_cup - t_hand <= T_hand_to_cup_max`

4) `S3_CUP_MOUTH_DONE -> S4_SWALLOW_DONE`
   - `E_SWALLOW` 발생  
   - 조건: `t_swallow - t_cup <= T_cup_to_swallow_max`

### Timeouts
- `T_hand_to_cup_max = 6s` (손→컵까지)
- `T_cup_to_swallow_max = 4s` (컵→삼킴까지)
- `T_overall_max = 15s` (전체 시퀀스)

Failure rules:
- roi_valid가 INVALID로 오래 가면 상태 유지/중지
- timeout 초과 시 `Sx_FAIL`로 이동 후 reset

---

## 5) Final Decision Logic

### Output = O (복용 완료)
- `S4_SWALLOW_DONE` 도달

### Output = X (미복용)
- 전체 window 동안 `E_CUP_MOUTH`가 없거나,
- `E_CUP_MOUTH`는 있으나 swallow 조건 실패 + timeout

### Output = UNKNOWN (판정 불가)
- 얼굴/ROI가 장시간 invalid (roi_valid=False)
- 컵/손 탐지 신뢰도 지속적으로 낮음

---

## 6) Robustness Rules (중요)

1) **얼굴이 없을 때 고정 ROI 사용 금지**
- roi_invalid이면 이벤트 감지 중지 (UNKNOWN로만)

2) **Multi-frame confirmation**
- hand/cup event는 반드시 연속 프레임 조건

3) **Smoothing**
- mouth_roi EMA smoothing
- object tracker(손/컵)도 가능하면 간단한 Kalman/EMA

4) **Debug overlay**
- mouth_roi, hand_bbox, cup_bbox
- state, event timestamps, scores를 프레임에 표기

---

## 7) Implementation Deliverables (Cursor가 만들어야 할 것)

### Files / Classes
- `mouth_roi_tracker.py`
  - `MouthROITracker.update(frame) -> MouthROIResult`
- `hand_tracker.py`
  - `HandTracker.update(frame) -> HandResult`
- `cup_detector.py`
  - `CupDetector.update(frame) -> CupResult`
- `swallow_estimator.py`
  - `SwallowEstimator.update(frame, mouth_landmarks) -> SwallowResult`
- `pill_intake_fsm.py`
  - `PillIntakeFSM.update(features, timestamp) -> Decision`
- `main.py`
  - webcam loop + debug overlay + logging

### Unit Tests
- ROI invalid handling (고정 화면 ROI 금지)
- FSM transition test (synthetic events)
- timeout behavior test

### Logging
- JSONL 이벤트 로그:
  - `{t, state, events, roi_mode, scores}`

---

## 8) Default Parameters (초기값)
- `alpha_roi = 0.5`
- `hold_last_roi_frames K = 15` (30fps 기준 0.5s)
- `F_min_frames = 5`
- `H_min_frames = 6`
- `C_min_frames = 10`
- `S_th = 0.7`
- `T_hand_to_cup_max = 6.0s`
- `T_cup_to_swallow_max = 4.0s`
- `T_overall_max = 15.0s`

---

## 9) Notes / Assumptions
- 카메라 구도: 상반신 + 얼굴(대부분 프레임에서 보임)
- 알약은 직접 검출하지 않으며, **hand→mouth 이후 cup→mouth→swallow**로 복용을 판정한다.
- 컵이 인식되지 않는 환경이라면 cup detector는 custom 학습 필요.

---