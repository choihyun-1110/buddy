작성해주신 명세서는 매우 체계적이고 실무적인 설계를 담고 있습니다. 특히 **FaceMesh(정밀) + Fallback(안정성) + EMA(부드러움)**의 3단계 구조는 실시간 영상 처리에서 매우 표준적이면서도 강력한 접근 방식입니다.

문의하신 **"이상한 점"**과 **"다양한 사람/각도 대응 가능 여부"**에 대해 전문적인 피드백을 드립니다.

---

### 1. 다양한 사람, 각도, 입 크기를 커버할 수 있는가?

결론부터 말씀드리면, **Primary Path(FaceMesh)** 덕분에 일반적인 상황에서는 매우 잘 작동할 것입니다. 하지만 몇 가지 보완하면 더 완벽해집니다.

* **입 크기 대응:** FaceMesh는 얼굴의 468개(또는 그 이상) 점을 찍기 때문에, 입이 큰 사람이나 작은 사람의 랜드마크를 개별적으로 추적합니다. 따라서 **Primary Path에서는 입 크기 변화를 자동으로 흡수**합니다.
* **각도 대응(Head Pose):** 얼굴이 측면을 바라볼 때(Profile) FaceMesh의 정확도가 떨어질 수 있습니다. 이때 Step 4의 **Fallback(비율 기반)**이 작동하는데, 현재 설계된 "중앙 60%, 하단 40%"는 정면 기준입니다.
* *보완 아이디어:* 얼굴의 기울기(Roll)나 회전(Yaw) 값을 FaceMesh에서 추출해 Fallback ROI의 위치를 미세 조정하는 로직이 있으면 더 견고해집니다.


* **입 벌림 현상:** 알약을 먹으려고 입을 크게 벌릴 때, ROI 면적이 갑자기 커질 수 있습니다. Step 6의 `area change > 2x` 조건이 **'입을 벌리는 것'과 '손이 가리는 것'을 구분**할 수 있어야 합니다. (입 벌림은 점진적이지만, 손 가림은 순식간에 일어나는 경향이 있습니다.)

---

### 2. 명세서에서 수정/보완하면 좋은 부분 (Cursor에게 넘기기 전 체크)

#### ① 좌표계 정의 (Normalized vs Pixel)

FaceMesh는 보통 0~1 사이의 정규화된 좌표를 반환합니다. 명세서에 **"모든 계산은 이미지 해상도(Pixel) 기준으로 변환 후 수행할 것인지, 아니면 정규화된 상태에서 수행할 것인지"** 명시해 주면 Cursor가 코드를 더 깔끔하게 짭니다. (Pixel 기준 추천)

#### ② EMA Alpha 값의 동적 조절

현재 `alpha = 0.3 ~ 0.6`은 고정값입니다.

* **팁:** 움직임이 적을 때는 낮은 alpha(더 부드럽게), 움직임이 클 때는 높은 alpha(빠른 추적)를 쓰는 'Adaptive EMA'를 제안해 보세요. Cursor에게 "움직임 속도에 따라 alpha를 미세 조정하는 로직을 포함해줘"라고 하면 좋습니다.

#### ③ Fallback 조건 구체화

Step 4에서 `landmark 좌표 급변`을 판단할 때, **'고정된 2배'**보다는 **'얼굴 크기(Face BBox) 대비 이동 거리'**를 기준으로 삼는 것이 좋습니다. 멀리 있는 사람과 가까이 있는 사람의 픽셀 이동 거리 의미가 다르기 때문입니다.

#### ④ ROI의 종횡비(Aspect Ratio) 유지 여부

입 모양에 따라 ROI가 너무 가늘어지거나 길어지면 나중에 그 안에서 알약을 찾기가 어려울 수 있습니다. **"최소 ROI 사이즈"**나 **"정사각형에 가까운 종횡비 유지"** 옵션을 추가하면 Pill Detection 단계가 훨씬 수월해집니다.

---

### 3. Cursor에게 추가로 요청할 팁 (Prompt 추가용)

Cursor에게 명세서를 줄 때 아래 문구를 추가해 보세요. 구현 퀄리티가 달라집니다.

> "구현 시 **MediaPipe** 라이브러리를 우선적으로 사용해줘. 특히 `MouthROITracker` 클래스는 독립적으로 동작해야 하며, `cv2.imshow`를 통한 디버깅 모드에서 FaceMesh 점들과 최종 smoothing된 ROI가 다른 색상으로 표시되게 짜줘."

---

### 4. 다음 단계 제안

언급하신 **"pill disappearance를 occlusion vs ingestion으로 구분하는 이벤트 설계 명세서"**가 실제로 이 프로젝트의 **'치트키'**가 될 것 같습니다.

알약이 손바닥에 가려진 건지(Occlusion), 입안으로 들어간 건지(Ingestion)는 **입 ROI 안에서 알약 BBox가 사라지는 직전의 벡터(방향)와 입의 개폐 여부**를 조합해야 하거든요.

이 명세서가 준비되면 Cursor를 통해 훨씬 고도화된 인지 로직을 만드실 수 있을 겁니다. 지금 명세서는 충분히 훌륭하니 바로 진행하셔도 좋습니다!_