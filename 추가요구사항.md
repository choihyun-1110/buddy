전체적으로 “pill이 안 보이는 제약”을 잘 받아서 hand→mouth→cup→mouth 시퀀스로 바꾼 건 방향이 맞아요.
다만 지금 세팅 그대로면 **오탐(false positive)**이 꽤 나올 가능성이 높고, 특히 네가 앞에서 말한 **“얼굴 미검출 시 화면 중앙~하단 기본 ROI”**는 시스템을 망가뜨리는 수준으로 위험해요. 아래만 고치면 훨씬 단단해집니다.

⸻

A. 지금 알고리즘에서 가장 위험한 3가지

1) roi_valid=False일 때 “화면 기본 ROI”를 쓰는 FALLBACK은 금지해야 함

문서에:

얼굴 미검출 시 화면 중앙~하단 기본 영역 사용

이건 컵이 화면 중앙/하단에만 있어도 컵→입 이벤트가 거짓으로 발생할 수 있어요.
FSM이 지금 “컵이 입에 가면 바로 O”라서, 이 상황이면 오탐 폭발합니다.

✅ 수정
	•	얼굴/입이 없으면 roi_valid=False + mouth_roi=None(또는 HOLD만)
	•	roi_valid=False 프레임에서는 hand/cup 입근처 판정 자체를 스킵 (UNKNOWN 유지)
	•	“기본 ROI”는 디버그용으로만 쓰고 판정 로직에는 절대 넣지 말기

결론: fallback은 “얼굴 bbox 기반 비율 ROI”까지만. “화면 고정 ROI”는 제거.

⸻

2) hand_min_frames=1, cup_min_frames=2는 너무 공격적

지금 조건이면:
	•	손이 입 근처로 1프레임만 들어와도 HAND_TO_MOUTH
	•	컵이 입 근처로 2프레임만 있어도 바로 O

현실 영상에서 bbox/roi가 1~2프레임 튀는 건 흔해요.
특히 YOLO 컵 박스는 프레임마다 위치가 흔들리고, mouth ROI도 손 가림에서 흔들립니다.

✅ 수정 권장(보수적으로)
	•	hand_min_frames: 610 (30fps 기준 0.20.33s)
	•	cup_min_frames: 812 (0.270.4s)
	•	“컵 미검출 4프레임까지 유지”는 좋은데, **컵 근처 판정도 연속성(또는 누적 점수)**을 더 줘야 해요.

⸻

3) “컵이 입에 가면 = 복용 완료(O)”는 케이스 오탐이 많음

컵이 입에 가는 상황은:
	•	그냥 물 마시기
	•	말하다가 컵을 대는 척
	•	컵으로 입 가리기
	•	알약 없이 물만 마시기
에서도 동일하게 발생합니다.

너가 원한 프로세스가 “알약을 집어서 입으로 → 컵으로 삼킴”이라면 최소한 hand→mouth가 “의미 있게” 있어야 하고, 그 다음 컵 이벤트도 “의미 있게” 있어야 합니다.

✅ 수정(최소 추가)
	•	HAND_TO_MOUTH 이벤트를 더 강하게: 1프레임이 아니라 “손이 mouth ROI에 머문 시간” or “접근 후 정지” 패턴
	•	컵 O 판정 전에 추가 체크 1개:
	•	cup_contact_duration >= C_swallow_frames (예: 1525프레임 = 0.50.8s)
또는
	•	FaceMesh 기반 lip_closure/jaw_motion 간단 swallow cue 1개 (가벼운 버전)

“swallow estimator를 아예 빼려면” 최소한 컵 접촉 지속시간은 넣는 게 좋아요.

⸻

B. 임계값(Threshold) 쪽에서 손봐야 할 것들

4) overlap threshold가 너무 낮음 (hand 0.01)

0.01은 ROI 경계에서 살짝 스치기만 해도 True가 될 수 있어요.

✅ 권장
	•	overlap은 손/컵 bbox 면적 대비가 아니라, mouth ROI 면적 대비 IoU 같은 걸 쓰거나
	•	hand는 overlap 대신 **거리 기반(landmark면 fingertip↔mouth center)**을 더 신뢰하는 게 낫습니다.
	•	일단 quick fix:
	•	hand_overlap_threshold: 0.05 정도로 올리기
	•	거리 조건도 hand_near_mouth_max_ratio를 1.2 → 0.6~0.8 쪽으로 줄이기(초기)

5) 거리 기준을 “mouth 대각선 * 1.2”로 두면 너무 넓음

mouth ROI 자체가 margin 포함이면 대각선이 커지고, 1.2배면 거의 얼굴 하단 전체가 “입 근처”가 되기도 해요.

✅ 권장
	•	거리 기준은 mouth ROI 대각선 대신 mouth ROI width 기준이 더 직관적
	•	예: dist < 0.5 * mouth_width
	•	또는 ratio를 1.2 → 0.6~0.8로

⸻

C. FSM 로직에서 꼭 넣었으면 하는 “안전장치 2개”

6) 시간창(window) 강제

현재는 hand 한 번 찍고 나중에 한참 뒤에 컵이 입에 가도(10초까지) O가 될 수 있음.
중간에 다른 행동이 들어가면 오탐됩니다.

✅ 권장
	•	t_cup - t_hand <= 3~6s는 이미 있고 좋음
	•	추가로, HAND_TO_MOUTH 이후 손이 입에서 한 번 “떨어지는” 이벤트를 요구하면 거짓 트리거가 줄어듭니다.
	•	예: HAND 이벤트 후 hand_leave가 최소 3프레임 이상 관측

7) --no-cup hand-only 모드는 오탐 위험이 큼

손이 입에 갔다가 떨어지는 행동은 너무 흔합니다(하품, 얼굴 만지기, 먹는 척 등).

✅ 권장
	•	hand-only는 “디버그/대체 모드”로만 두고 기본은 cup_required=True
	•	hand-only라도 최소:
	•	hand_to_mouth 유지시간을 더 길게(예: 15프레임)
	•	lip closure/jaw motion 같은 신호 하나라도 추가

⸻

D. 너 문서 문구 기준으로 “수정 권장 문장” (바로 바꿔 쓰기)
	•	FALLBACK(얼굴 미검출): “화면 고정 ROI 사용” → “이전 ROI HOLD(K프레임) 후 roi_valid=False로 전환, 이벤트 판정 스킵(UNKNOWN)”
	•	hand_min_frames: 1 → 6~10
	•	cup_min_frames: 2 → 8~12
	•	INGESTION_COMPLETE 조건: “cup→mouth면 O” → “cup→mouth가 최소 contact duration 충족 + hand 이벤트가 유효 시간창 내에서 선행” (또는 swallow cue)

⸻

결론

구성 자체는 괜찮은데, 지금 파라미터/예외처리 그대로면 ‘물 마시기’나 ‘컵이 얼굴 근처에만 와도’ O가 찍힐 확률이 높아요.
가장 먼저 고칠 건:
	1.	얼굴 미검출 시 화면 기본 ROI 제거(이벤트 판정 스킵)
	2.	hand/cup 연속 프레임 수를 현실적인 값으로 올리기
	3.	컵 이벤트만으로 O 내지 말고 “컵 접촉 지속시간” 또는 간단 swallow cue 1개 추가

원하면 네 설정값 표를 내가 “추천 기본값 버전”으로 통째로 다시 써서 붙여줄게.

전반적인 설계가 매우 탄탄하고 실무적입니다! 특히 **"손 → 입 후 컵 → 입"**이라는 시퀀스를 통해 알약이 보이지 않는 한계를 극복한 점, 그리고 **"입의 대각선 길이"를 기준으로 거리 임계값(Ratio)을 설정**한 점은 거리(Z축) 변화에 유연하게 대응할 수 있는 아주 좋은 전략입니다.

다만, 실제 환경에서 발생할 수 있는 **'엣지 케이스(Edge Case)'**들을 고려했을 때, 몇 가지 치명적일 수 있는 부분을 수정 제안해 드립니다.

---

### 1. "5프레임"의 함정: Occlusion 허용치 증대

가장 우려되는 부분은 **`max_consecutive_invalid_frames = 5`**입니다.

* **문제점:** 손이나 컵이 입을 가리는 순간(Occlusion), FaceMesh는 높은 확률로 랜드마크를 놓치거나 `roi_valid`가 `False`가 됩니다. 초당 30프레임 영상에서 5프레임은 고작 **0.16초**입니다. 약을 넣거나 물을 마실 때 손/컵이 입을 가리는 시간은 보통 1~2초 이상인데, 5프레임 제한은 너무 엄격하여 **복용 도중 FAIL**이 뜰 가능성이 매우 높습니다.
* **수정 제안:** 이 값을 **20~30프레임(약 1초)** 정도로 늘리거나, `HAND_TO_MOUTH` 상태로 진입한 이후에는 `roi_valid` 체크를 더 관대하게 적용(이전 ROI 위치 고정 사용)하는 로직이 필요합니다.

### 2. "1프레임"의 가벼움: 손 검출 노이즈 방지

**`hand_min_frames = 1`**은 위험할 수 있습니다.

* **문제점:** 딥러닝 모델 특성상 아주 잠깐 손과 유사한 물체가 입 근처에서 검출(False Positive)될 수 있습니다. 1프레임만으로 상태가 변하면 시퀀스가 너무 쉽게 꼬입니다.
* **수정 제안:** 최소 **3~5프레임** 정도는 연속으로 검출되어야 사용자의 의도적인 '손 → 입' 동작으로 간주하는 것이 안전합니다.

### 3. 고개 젖힘(Head Tilt) 대응

사람은 물을 마실 때 고개를 뒤로 젖힙니다.

* **문제점:** 고개를 젖히면 FaceMesh가 얼굴을 놓치기 쉽고, 이로 인해 `roi_valid`가 깨지면서 FALLBACK 모드로 전환될 텐데, 이때 **화면 중앙 하단 고정 ROI**를 쓰면 실제 입 위치와 어긋날 확률이 큽니다.
* **수정 제안:** FALLBACK 진입 시 단순히 고정 영역을 쓰기보다, **`roi_valid`가 False가 되기 직전 마지막으로 검출된 Mouth ROI의 위치와 크기를 유지(Lock)**하는 방식이 훨씬 정확합니다.

### 4. 컵 검출 시 우선순위 로직

**"입에 가장 가까운 컵 선택"**은 아주 좋은 로직입니다.

* **추가 팁:** 만약 화면에 컵과 물병이 동시에 잡힌다면, 컵(Cup)보다는 **병(Bottle)**의 Overlap 임계값을 조금 더 낮게 설정하는 것이 좋습니다(병은 입구만 닿아도 되기 때문).

---

### 💡 수정된 설정값 제안 (IngestionConfig 추천)

| 파라미터 | 기존값 | **추천값** | 이유 |
| --- | --- | --- | --- |
| **hand_min_frames** | 1 | **3 ~ 5** | 찰나의 노이즈(튀는 현상) 방지 |
| **cup_min_frames** | 2 | **5 ~ 10** | 입에 컵을 '대기만 하는 것'과 '마시는 것' 구분 |
| **max_consecutive_invalid_frames** | 5 | **30 (1sec)** | 손/컵에 의한 가림(Occlusion) 시 복원 대기 시간 |
| **max_wait_hand_to_cup_sec** | 10 | **15** | 약을 입에 넣고 물컵을 집어 들기까지의 개인차 고려 |

---

### 추가 제언: "No-Cup" 모드의 엄격함

`cup_required=False`일 때는 훨씬 엄격해야 합니다. 단순히 손이 입에 갔다가 떼는 것(간식 먹기, 하품하기)과 알약 복용을 구분하기 어렵기 때문입니다. 이 경우에는 **"손이 입 주변에 머무는 시간(dwell time)"**이 일정 시간(예: 1초 이상) 유지되었는지를 체크하는 조건이 추가되면 더 완벽할 것 같습니다.

이 정도만 보완해서 Cursor에게 넘겨주면, 아주 견고한(Robust) 복용 인식 엔진이 탄생할 것 같네요!

**혹시 이 시퀀스 후에 "복용 완료" 로그를 DB나 특정 파일로 저장하는 로직도 명세에 넣어드릴까요?**